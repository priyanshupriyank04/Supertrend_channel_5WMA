# Core dependencies
pandas>=1.5.0
psycopg2-binary>=2.9.5
kiteconnect>=4.1.0
requests>=2.28.1

# Optional but recommended for real-time trading systems
websocket-client>=1.5.0     # For Zerodha WebSocket streaming (if used later)
python-dotenv>=1.0.0        # For managing .env API secrets securely
schedule>=1.1.0             # For scheduled tasks like re-authentication
retrying>=1.3.4             # For retry logic in API calls
numpy>=1.23.0               # For numeric operations / indicators
sqlalchemy>=2.0.0           # For flexible DB interactions beyond psycopg2

# Logging & Debugging
loguru>=0.7.0               # Better structured logging (alternative to logging module)

# System level and timezone handling
pytz>=2023.3                # For timezone-aware datetime processing

# Dev & compatibility
typing-extensions>=4.5.0    # For type hint support across versions
